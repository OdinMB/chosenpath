/**
 * What people are told when moderation blocks their content: the owner's
 * approved copy of 2026-09-25 (CP-9), which says the check is automated and
 * uses AI. Shown by the live notification (apiClient) and by
 * ContentModerationNotification.
 */
export const MODERATION_MESSAGE =
  "Our automated moderation, which uses AI, flagged your content, so we couldn't process it.";
